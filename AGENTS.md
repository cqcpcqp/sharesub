# AGENTS.md

## 强制规则

- 永远不要尝试打开浏览器进行测试。
- 后端接口返回的数据结构和字段类型是固定的；不要为后端接口返回实现兜底机制。
- 不猜测接口，必须查询并复用现有接口。
- 业务含义不明确时，必须向用户确认。
- 主动运行与改动风险相匹配的测试，遵循现有架构和项目规范。

## 版本发布与生产环境

- 用户所说的“发布版本”“发版本”默认仅指发布项目代码版本，不代表部署到生产环境。
- 代码版本发布通常包括：更新项目版本号、创建提交、推送代码、创建并推送与 `VERSION` 一致的 annotated tag，以及检查相关 Git/CI 状态。
- 不得因为用户要求“发布版本”“发版”“commit & push”而执行 `make deploy`、触发生产部署工作流、连接生产服务器、修改生产配置、切换生产镜像、执行生产数据库迁移或以任何其他方式改变生产环境。
- 任何生产部署或生产环境操作都必须由用户单独、明确地提出；只要涉及生产数据库迁移、数据恢复、删除或其他不可逆风险，还必须在执行前再次说明具体影响并获得针对该操作的明确确认。
- Git tag、GitHub Release、CI 镜像构建和生产部署是不同操作，不得混为一谈。
