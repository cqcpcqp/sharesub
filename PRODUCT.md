# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

主要用户是熟人之间组成的共享关系和自建小团队。他们共同使用一个自己拥有或已获得明确授权使用的 OpenAI Codex 账号：房主负责接入账号、建立共享边界和管理成员，成员需要各自独立的使用入口，并清楚了解自己的用量和可用额度。

公开招募成员的房主是次要用户。他们可以把预留席位发布到大厅，但申请人仍需经过房主审批才能加入。

平台管理员负责处理 ShareSub 自身的用户、OpenAI 账号、Plan 和 API Key 运营管理。

## Product Purpose

ShareSub 让有权使用同一 Codex 账号的成员通过 Plan 建立清晰、可控的协作关系。每位成员使用自己的 API Key，房主控制账号、成员和共享方式，所有参与者都能查看与自身权限相关的额度和性能信息。

产品成功意味着熟人或小团队不必共享同一份访问密钥，也能共同使用 Codex 账号；账号归属、成员关系、额度边界、路由规则和实际消耗始终清楚可查。

## Positioning

ShareSub 让有权使用同一 Codex 账号的成员，通过独立 API Key、透明额度和可控路由进行安全协作。它不是通用大模型网关或交易市场，而是专门解决 Codex 账号在可信关系和小团队中的共享、分配与可见性问题。

## Operating Context

- 房主通过 OpenAI OAuth 接入自己拥有或获授权使用的 Codex 账号，并为账号配置名称、备注、代理、并发和 RPM 限制。
- 房主可以先创建 Plan 并在固定份额或共享使用两种额度方式中选择一种，再在 Plan 内绑定已有 OpenAI 账号或接入新账号。
- 私密协作通过七天内有效、只能领取一次的邀请链接建立；公开协作通过大厅申请和房主审批建立。
- 成员创建自己的 `sk-sharesub-...` API Key，并可把一个 Key 绑定到多个已加入的 Plan，按优先级或可用额度均衡选路。
- 成员使用 Codex 兼容入口发起请求，并通过个人仪表盘和 Plan 数据查看 Token、额度窗口、性能与成员用量。
- 产品部署为 Web 应用，前端使用 Vue 3、TypeScript 和 Vite，后端使用 Go、PostgreSQL，并可通过 Docker Compose 与 Nginx 部署。

## Capabilities and Constraints

- 仅支持 OpenAI Codex OAuth 账号，不支持 Anthropic、Gemini、Bedrock 或通用供应商抽象。
- 一个 Plan 最多绑定一个由房主拥有的 OpenAI 账号；未绑定账号时可以探索成员与设置，但不能公开发布、配置 API Key 路由或转发请求。Plan 可以是公开或私密。
- Plan 创建时确定固定份额或共享使用模式，创建后不能更改。
- 额度同时按照 Codex 的五小时和七天窗口执行；固定份额模式限制成员额度，共享模式只限制账号总额度。
- 每个 API Key 归创建它的用户所有，可以绑定多个用户已加入的 Plan，并使用优先级或均衡策略选路。
- 产品不包含计费、支付、转售市场或通用渠道调度能力。
- 网关不把请求或响应正文写入数据库或性能记录；敏感凭据和完整密钥按照现有安全模型加密或哈希保存。
- 后端接口字段、字段类型和响应结构是固定契约。前端直接使用与契约一致的类型，不猜测返回结构，不添加响应兜底或兼容分支。
- ShareSub 是独立产品，与 OpenAI 无隶属、授权或代理关系；用户只能接入自己拥有或已经获得明确授权使用的账号。

## Brand Commitments

- 产品名称 `ShareSub` 和标语 `Access together` 是长期保留的品牌事实。
- 当前 Logo 文件位于 `frontend/public/brand/`，但用户明确不满意现有 Logo。它们是临时资产，不应被当作未来重设计必须延续的视觉权威。
- 新 Logo 的具体方向尚未决定；后续设计流程必须单独确认，不能在产品上下文中臆造。

## Evidence on Hand

- `README.md`、`docs/architecture.md`、`docs/api.md` 和 `docs/forwarding-compatibility.md` 记录了当前已实现的产品能力、接口和系统边界。
- `frontend/src/views/PublicHomeView.vue` 包含基于真实产品能力编写的公开介绍、安全说明和常见问题。
- `frontend/public/brand/` 包含当前临时 Logo、标志和 favicon 资产。
- 仓库包含前后端实现与自动化测试，可以验证功能和技术声明。
- 当前没有已确认的客户证言、客户 Logo、新闻报道、外部评测或商业数据；未来界面和文案不得捏造这些证据。
- 首页中的成员、Plan 和用量数据是功能示意，不是真实客户或公开 Plan 证据。

## Product Principles

1. **可信关系优先。** 首先服务熟人和自建小团队的协作，公开招募是受房主审批约束的次要场景。
2. **共享访问不等于共享身份。** 账号由房主管理，每位成员保有独立密钥、用量归属和可撤销的访问关系。
3. **边界必须清楚。** 成员、额度、路由、并发和生命周期规则应当可见、可解释且确定执行。
4. **用实现建立信任。** 只陈述已经实现并能验证的安全、隐私和透明能力，不用模糊承诺或虚构证据包装产品。
5. **尊重稳定契约和明确范围。** 不猜测接口、不为固定响应添加兜底，也不把产品扩展成未确认的通用平台、支付系统或交易市场。
