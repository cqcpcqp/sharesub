# ShareSub 工程协作规范

本仓库的实现必须同时遵守根目录 `AGENTS.md`、`docs/architecture.md`、`docs/api.md` 和本文。发生冲突时，业务契约与 `AGENTS.md` 优先。

## 不可突破的边界

- 不猜测后端接口，不新增后端响应兜底、兼容字段或运行时结构推断。
- 重构必须保持接口、业务行为、错误语义、持久化结构和迁移历史不变。
- 已提交的 migration 不重命名、不修改、不重排；数据库变化必须使用新的 migration，并单独评审。
- “发布代码版本”和“部署生产环境”是不同操作，遵守 `AGENTS.md` 的授权边界。
- 业务含义不明确时先确认，不通过代码结构臆造业务规则。

## 依赖方向与模块边界

### 后端

后端保持 `domain -> application -> infrastructure`：

- `internal/domain` 只放稳定业务模型、值语义和领域错误，不依赖 HTTP、PostgreSQL 或 OpenAI 客户端。
- `internal/application` 组织用例，并通过按能力划分的小接口声明依赖。
- `internal/httpapi` 负责协议解析、鉴权、状态码和响应写入，不实现业务决策。
- `internal/postgres` 负责 SQL、事务和持久化映射，不承担 HTTP 或上游协议逻辑。
- `internal/openai` 负责上游协议、转发和计量；会话编排、帧解析、拨号器和响应计量应分文件组织。

新增用例优先依赖最小能力接口。不要把新方法直接堆入无边界的通用 Store 或 Service。

### 前端

- `views/` 负责页面组合，不直接承载一个业务域的全部状态、命令和格式化规则。
- 可复用 UI 放入 `components/`；页面专属状态放在同目录 composable 或 feature 模块中。
- `api/` 按业务域组织请求；固定响应直接使用 `types.ts` 导出的契约类型。
- 视图不得转换、猜测或兜底后端响应结构。
- 纯格式化、标签映射和校验函数与异步副作用分离，并应可独立测试。
- 重构组件时保持已有 DOM 语义、可访问性、主题 Token 和响应式行为。

## 规模约束

行数是审查触发器，不是拆文件目标。拆分必须形成清晰职责，禁止仅按行号切出 `helpers2`、`misc` 等无语义模块。

### 硬限制

- 新增生产代码文件不得超过 700 行。
- 已进入 `scripts/code-size-baseline.txt` 的遗留文件不得增长；完成拆分后必须降低或删除对应基线。
- 新增单个函数原则上不得超过 100 行；超过时必须在评审中说明状态机或协议完整性理由。
- 新增 application 端口接口原则上不超过 20 个方法；用例应依赖更小的能力接口。
- 新增 migration 的三位数字序号必须唯一。

### 软限制

- 生产文件超过 400 行时，应检查是否混合了多个变化原因。
- Vue SFC 超过 400 行时，优先拆分业务区块、composable 或页面专属样式。
- Go 文件超过 500 行时，优先按同 package 内的协议、状态机、存储查询或传输职责拆分。
- 测试文件可超过生产文件阈值，但应按被测能力分组，避免一个测试文件覆盖无关业务域。

## 数据治理

- 数据字段、单位、时间窗口和聚合口径以 `docs/architecture.md` 与 `docs/api.md` 为准。
- 百分比、Token 和费用继续使用既有整数单位，不引入浮点累计。
- 敏感凭据继续遵守既有加密/哈希模型；网关不持久化完整请求或响应正文。
- 新增持久化数据前必须明确：业务所有者、敏感等级、保留周期、删除条件、访问角色和派生口径。
- API 契约变更必须同步 Go 类型、前端类型、`docs/api.md` 和对应测试，但不得通过前端 fallback 兼容未确认结构。

更完整的数据分类、生命周期和责任清单见 `docs/data-governance.md`。

## 本地验证

提交前至少运行：

```bash
make quality
```

该命令执行代码规模、migration 命名、格式、静态检查、后端测试、前端测试、类型检查和生产构建。涉及 PostgreSQL 行为时，还必须设置 `SHARESUB_TEST_DATABASE_URL` 运行后端集成测试。

## 重构提交要求

- 结构重构与业务变更分开提交。
- 先增加或确认覆盖关键行为的测试，再移动代码。
- 保留公共导出、错误类型、JSON 字段、路由和事件名称。
- 不以“顺便清理”为理由改文案、样式、数据库或接口。
- PR 描述应列出移动前后的职责边界和已运行的验证命令。
